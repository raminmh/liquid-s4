# @package _global_
defaults:
  - /pipeline: pathfinder
  - /model: s4
  - override /scheduler: cosine_warmup

scheduler:
  num_training_steps: 250000 # 200 epochs (only 100 p edit)
  num_warmup_steps: 2500 # 1 epoch

model:
  dropout: 0.0
  n_layers: 6
  prenorm: false # p edit
  d_model: 256
  norm: batch
  layer:
    d_state: 64
    lr: 0.001
    dt_min: 0.001
    dt_max: 0.1
    measure: legs
    bidirectional: false # p edit
    postact: glu
    n_ssm: 256

decoder:
  mode: last

loader:
  batch_size: 256

optimizer:
  lr: 0.004
  weight_decay: 0.03

trainer:
  max_epochs: 100 # from 200 p edit

train:
  seed: 2222
  interval: step
